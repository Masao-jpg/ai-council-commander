# Google Gemini API Key
# Get your key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Server Port
PORT=3001

# Mock Mode (for testing without API key)
# Set to 'true' to use simulated responses
USE_MOCK=false

# Google Service Account for Google Docs Export (Optional)
# 1. Go to: https://console.cloud.google.com
# 2. Create or select a project
# 3. Enable Google Docs API and Google Drive API
# 4. Create a Service Account and download JSON key file
# 5. Local: Set the path to the JSON file below (place in server/ directory)
# 6. Render: Use Secret Files to upload the JSON file with filename: google-credentials.json
#           Then set GOOGLE_SERVICE_ACCOUNT_KEY=google-credentials.json
# Example (Local): GOOGLE_SERVICE_ACCOUNT_KEY=./google-credentials.json
# Example (Render): GOOGLE_SERVICE_ACCOUNT_KEY=google-credentials.json
GOOGLE_SERVICE_ACCOUNT_KEY=

# Google Drive Folder ID (Optional but recommended)
# 1. Open the Google Drive folder where you want to save documents
# 2. Copy the folder ID from the URL: https://drive.google.com/drive/folders/FOLDER_ID_HERE
# 3. Share the folder with the service account email (as Editor)
# 4. Set the folder ID below
# Example: GOOGLE_DRIVE_FOLDER_ID=1A2b3C4d5E6f7g8h9i0j
GOOGLE_DRIVE_FOLDER_ID=
